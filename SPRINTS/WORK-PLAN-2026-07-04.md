# План работ (составлен 2026-07-04, вечер)

**Основание:** ревизия PROJECT_STATUS.md, SPRINT-050/052/053/054-PLAN, аудита 2026-07-04 + **живая проверка `main` и CI** (GitHub Actions, воспроизведение ошибок локально на HEAD `f6a9b9f`).

---

## 1. Где проект сейчас

**Сделано (спринты 042–049 + 052):** type safety доведён до 0 `any` (из 342), 0 нарушений слоёв, eager-load главной 1.19 МБ → ~508 КБ gzip, per-version лайки в проде, мобильный UX-аудит (045–049), Suno gap закрыт на 24/28 категорий (052 добавил Mashup / Persona / File Upload). Метрики кода — в порядке.

**Проблема — не код, а процесс и CI.** Фактическое состояние `main` на момент этого плана (проверено, не по докам):

| Workflow        | Статус на HEAD `f6a9b9f`                 | Причина                                                            |
| --------------- | ---------------------------------------- | ------------------------------------------------------------------ |
| Quality & Build | 🔴 failure                               | **8 ошибок `tsc`** в коде Sprint 052 (см. §2)                      |
| CI/CD Pipeline  | 🔴 failure (пред. коммиты) / in progress | typecheck + format-дрейф                                           |
| Docs            | 🔴 failure                               | lychee link-check (частично закрыт, остался `graphify-out/**`)     |
| E2E Tests       | 🟡 cancelled/in progress                 | вердикт после фикса `@axe-core/playwright` (#572) так и не получен |

Дополнительно зафиксировано: **history `main` перезаписывается force-push'ем** (локальный fetch показал `forced update 4ec3684...f6a9b9f`) — прямые коммиты Lovable идут мимо хуков и мимо PR. Это системная причина, по которой main снова и снова уходит в красное; Sprint 052 тоже влился с TS-ошибками.

**Рассинхрон нумерации:** SPRINT-050-PLAN в разделе «Следующие спринты» называет «Sprint 052 — Q4 Platform», но реальный 052 стал Suno API (уже завершён 8/10). Q4 Platform-блок (Export, Service Worker, Lighthouse CI, manualChunks, i18n) сдвигается на **Sprint 055**.

---

## 2. P0 — Хотфикс typecheck на `main` (сегодня, ~0.5 дня)

8 ошибок воспроизведены локально на HEAD:

1. `src/components/MashupDialog.tsx` — 6 ошибок: деструктуризация `data` из `useTracks()` (хук возвращает `tracks`, не `data`) + 5 implicit-`any` параметров как следствие.
2. `src/services/studio.service.ts:740, 771` — `SunoMashupParams` / `SunoPersonaParams` не имеют index signature, требуемой сигнатурой `invoke`-обёртки (лечится `satisfies`/распаковкой или расширением типа параметра обёртки).

Это блокер для всего остального: пока typecheck красный, ни один PR не видит честный сигнал CI. Unit-тесты в CI при этом даже не запускаются (step skipped после failure).

**Выход:** hotfix-PR → `main` зелёный по Quality & Build.

**✅ Выполнено (тот же день, ветка `claude/project-progress-sprint-plan-150yf8`):** обе правки внесены (`MashupDialog` — деструктуризация `tracks`; `SunoMashupParams`/`SunoPersonaParams` — `interface` → `type` для implicit index signature). Ошибка в `MashupDialog` была ещё и runtime-багом: пикеры треков Mashup всегда оставались пустыми. Верификация: `tsc` 0 ошибок, ESLint 0 ошибок, unit 292/292 (добавлен регрессионный тест `src/__tests__/components/MashupDialog.test.tsx`, 4 теста), production build + `size-limit` — все 9 бюджетов в норме, E2E deep-link смоук mashup — pass локально.

**🆕 Находки при верификации (входы в Sprint 050/051):**

1. **25 тест-файлов в `tests/unit/` никогда не запускаются**: `vitest.config.ts` включает только `src/**/*.test.{ts,tsx}`, тогда как CLAUDE.md заявляет паттерн `tests/unit/**` как рабочий. Либо расширить include (и починить то, что упадёт), либо перенести файлы в `src/__tests__/` — задача для Sprint 051 (Test Debt).
2. `tests/e2e/suno-mashup.spec.ts` использует `waitForLoadState("networkidle")` — хрупкий паттерн (не наступает при постоянных фоновых запросах Sentry/Supabase); в CI может флейкать. Кандидат на замену локатор-ожиданием в 050-A1.
3. Глобальный мок `ResizeObserver` в vitest setup — arrow-factory, не конструктор: любой компонент с Radix popper (`new ResizeObserver()`) в тестах падает. Локально обойдено в тесте; глобальный фикс — в Sprint 051.

---

## 3. Sprint 050 — Main Green + Mobile Audit F1–F12 (~10 дней)

Действует существующий [SPRINT-050-PLAN.md](SPRINT-050-PLAN.md) с корректировками по фактам:

### Фаза A: Main Green (дни 1–5) — порядок изменён

| #            | Задача                                                                                                                                                                          | Уточнение по фактам 2026-07-04                                                                                    |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- |
| A0 _(новое)_ | P0-хотфикс из §2                                                                                                                                                                | до всего остального                                                                                               |
| A4 ⬆️        | **Процесс Lovable → первым приоритетом**: branch protection на `main` (запрет force-push + required checks) ИЛИ auto-format/auto-fix workflow. Решение согласовать с владельцем | Force-push в `main` зафиксирован фактически; без этого фазы A1–A3 будут переделываться после каждого пуша Lovable |
| A1           | E2E вердикт: перезапустить suite на зелёном (после A0) main, чинить фактические падения                                                                                         | сбор suite уже починен в #572                                                                                     |
| A2           | Docs/lychee: судьба 4434 файлов `graphify-out/**` (`git rm -r --cached` либо убрать из `.gitignore`)                                                                            | живые ссылки уже починены follow-up к #573                                                                        |
| A3           | Сверка прод-миграций: `20260703120000` vs `20260704014859` (пересекающиеся likes-миграции), локальная накатка обеих                                                             |                                                                                                                   |
| A5           | `bun.lock` vs `package-lock.json` — один источник истины                                                                                                                        | зависит от решения по A4                                                                                          |

### Фаза B: F1–F12 + переносы (дни 6–10) — без изменений

- **B1** Scroll/viewport: `useScrollLock` (4 surfaces), `useVisualViewport`, SSR-guard (F1, F8, F9) — 1.5d
- **B2** Focus/keyboard: focus-trap, Library keyboard nav, Telegram BackButton race (F3, F6, F7) — 1.5d
- **B3** Поверхности: `PromptHistory` sub-dialog, QueueSheet toast/auto-close, `LibraryFilterChips` (F2, F5, F11) — 1d
- **B4** Данные/чистка: `usePublicTracks` cover_url, `VersionComparison` dead-code, `LazyImage` a11y (F4, F10, F12) — 1d
- **B5** ErrorBoundary home button (`useNavigate`, перенос из 045 D-4) — 0.5d
- **B6** Bundle quick wins: `lamejs`/`canvas-confetti`/`qrcode` → dynamic import (−150 КБ из `vendor-other`) — 0.5d

**DoD 050:** 4/4 workflow зелёные два дня подряд (включая при продолжающихся Lovable-пушах); 12 флагов закрыты или «won't fix» с причиной; total bundle < 2.11 МБ; миграции сверены и задокументированы; PROJECT_STATUS/ROADMAP обновлены.

---

## 4. Sprint 052-C — Cleanup хвостов 052 (параллельно с 050-B, ~1 день)

Deferred из завершённого 052: Storybook stories для `MashupDialog`, i18n strings (en/ru) для Mashup/Persona. Плюс ретро-фикс: разобраться, почему 052 влился без зелёного typecheck (вход в решение A4).

---

## 5. Sprint 051 — Test Debt + God Files (~10 дней, после 050)

Tests-first декомпозиция (порядок принципиален — сначала тесты, потом резка):

1. Unit-тесты для 20 `src/api/*.api.ts` + 18 `src/services/*.service.ts` (цель 282 → 450+ тестов).
2. Под защитой тестов — декомпозиция топ-3 из 9 файлов >800 LOC: `studio.service.ts` (1028), `LyricsParser.ts` (903), `studio.api.ts` (891).
3. Остальные 6 (>800 LOC): `IntegratedStemTracks.tsx`, `UnifiedNotesViewer.tsx`, `deeplink-tracker.ts`, `errorHandling.ts`, `AudioAnalysisService.ts`, `LyricsVisualEditor.tsx` — по остаточному времени или перенос в 055.

**DoD 051:** 450+ тестов зелёные, 0 файлов >1000 строк, покрытие API/сервисов ≥ смоук-уровня.

---

## 6. Sprint 053 — Suno Sounds + MIDI Direct + Boost Style (~8 дней, август)

По [SPRINT-053-PLAN.md](SPRINT-053-PLAN.md): `suno-sounds` (+callback), `suno-midi` (+callback, `midi-details`), `SfxGeneratorSheet`, MIDI fallback Suno→Replicate, судьба неподключённого `suno-boost-style` (подключить в `AudioActionDialog` или удалить), Telegram `/sfx`.

## 7. Sprint 054 — Suno Details Suite (~6 дней, сентябрь)

По [SPRINT-054-PLAN.md](SPRINT-054-PLAN.md): 6 details-edge через общий `_shared/suno-details.ts`, рефакторинг `suno-check-status` в диспетчер по `taskType`, generic `useSunoTaskDetails`, миграция 5 существующих polling-хуков. Итог: 28/28 категорий Suno API, error-rate polling <2%.

## 8. Sprint 055 — Q4 Platform (черновик, октябрь)

Бывший «052 — Q4 Platform» из заготовки 050-плана: Export service (WAV/MP3/FLAC), Service Worker + оффлайн, Lighthouse CI budget, ревизия `manualChunks` (2.11 МБ → ≤1.8 МБ), i18n (EN/RU).

---

## 9. Последовательность и зависимости

```
P0 hotfix (0.5d)
  └─► 050-A4 процесс Lovable (branch protection)  ◄── главный разблокирующий шаг
        └─► 050-A1..A3, A5 (Main Green)
              └─► 050-B1..B6 (F1–F12) ── параллельно ── 052-C cleanup
                    └─► 051 Test Debt + God Files
                          └─► 053 ─► 054 (Suno API 28/28)
                                └─► 055 Q4 Platform
```

## 10. Риски

| Риск                                                                     | Митигация                                                                        |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| Lovable продолжает force-push в `main` — любые фиксы CI перетираются     | 050-A4 первым; до включения protection — ежедневная сверка `git fetch` + ре-фикс |
| E2E может вскрыть реальные падения тестов (не только сборку)             | буфер 1–3 дня в 050-A1 уже заложен                                               |
| Две пересекающиеся likes-миграции могут конфликтовать при накатке с нуля | 050-A3: прогон на локальном стеке до любых новых миграций (053 добавляет свои)   |
| Декомпозиция god-файлов без тестов = регрессии                           | 051 строго tests-first                                                           |
| Нумерация спринтов расходится в 3 документах                             | при закрытии 050 — единая сверка PROJECT_STATUS / ROADMAP / SPRINT-PROGRESS      |
