/**
 * Hook for audio play counting, listen time tracking, and analytics events.
 *
 * This is a convenience re-export of usePlayerAnalytics, which already handles
 * all analytics concerns (play counts, listen duration, engagement tracking).
 */

export { usePlayerAnalytics as useAudioAnalytics } from "./usePlayerAnalytics";
