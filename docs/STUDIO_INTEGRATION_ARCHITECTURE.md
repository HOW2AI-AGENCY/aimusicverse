# 🏗️ Studio Integration Architecture - Visual Guide

**Дата:** 2026-01-04  
**Связано:** [STUDIO_INTEGRATION_PLAN_2026.md](./STUDIO_INTEGRATION_PLAN_2026.md)

---

## 📐 Архитектурная диаграмма

### Текущее состояние (BEFORE)

```
┌──────────────────────────────────────────────────────────────┐
│                  Разрозненные компоненты                      │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐  ┌─────────────────┐  ┌──────────────────┐
│ AudioRecord     │  │  GuitarStudio   │  │ Transcription    │
│ Dialog          │  │  Page           │  │ ExportPanel      │
│                 │  │                 │  │                  │
│ • Mic record    │  │ • Guitar        │  │ • MIDI export    │
│ • Cloud upload  │  │ • Klangio API   │  │ • Replicate API  │
│ • 5 min limit   │  │ • Chords        │  │ • Multiple       │
│                 │  │ • Analysis      │  │   formats        │
└─────────────────┘  └─────────────────┘  └──────────────────┘
        ↓                    ↓                      ↓
┌─────────────────────────────────────────────────────────────┐
│             ❌ Проблемы                                      │
│  • Дублирование кода (~30%)                                 │
│  • Нет единого workflow                                     │
│  • Фрагментированный UX                                     │
│  • Отсутствие визуализации (chords, MIDI roll)             │
└─────────────────────────────────────────────────────────────┘
```

### Целевое состояние (AFTER)

```
┌──────────────────────────────────────────────────────────────┐
│                   UnifiedStudioPage                           │
│                  (Entry Point)                                │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                     StudioShell                               │
│                  (Tab Container)                              │
│                                                               │
│  ┌──────────┐ ┌──────────┐ ┌────────────┐ ┌──────────┐     │
│  │ Tracks   │ │  Mixer   │ │ Recording  │ │  Export  │     │
│  │   Tab    │ │   Tab    │ │    Tab     │ │   Tab    │     │
│  │          │ │          │ │   (NEW)    │ │          │     │
│  └──────────┘ └──────────┘ └────────────┘ └──────────┘     │
└──────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴────────────────┐
                    ▼                                ▼
    ┌────────────────────────────┐   ┌─────────────────────────┐
    │ UnifiedRecordingStudio     │   │ TranscriptionWorkflow   │
    │                            │   │                         │
    │  ┌──────────────────────┐ │   │ ┌─────────────────────┐ │
    │  │  RecordingPanel      │ │   │ │ TranscriptionPanel  │ │
    │  │  • Source selector   │ │   │ │ • Engine selector   │ │
    │  │  • Multi-take        │ │   │ │ • Model selector    │ │
    │  │  • AI quality score  │ │   │ │ • Progress tracking │ │
    │  └──────────────────────┘ │   │ └─────────────────────┘ │
    │                            │   │                         │
    │  ┌──────────────────────┐ │   │ ┌─────────────────────┐ │
    │  │  MonitoringTools     │ │   │ │ ChordVisualization  │ │
    │  │  • Metronome         │ │   │ │ • Timeline overlay  │ │
    │  │  • Tuner             │ │   │ │ • Chord diagrams    │ │
    │  │  • Level meter       │ │   │ │ • Chord library     │ │
    │  │  • Pitch monitor     │ │   │ └─────────────────────┘ │
    │  └──────────────────────┘ │   │                         │
    │                            │   │ ┌─────────────────────┐ │
    │  ┌──────────────────────┐ │   │ │  MIDIRollViewer     │ │
    │  │  TakeManager         │ │   │ │  • Piano roll       │ │
    │  │  • Take list         │ │   │ │  • Drum roll        │ │
    │  │  • A/B comparison    │ │   │ │  • Guitar tab       │ │
    │  │  • Composite mode    │ │   │ └─────────────────────┘ │
    │  └──────────────────────┘ │   │                         │
    └────────────────────────────┘   └─────────────────────────┘
                    │                                │
                    ▼                                ▼
    ┌────────────────────────────┐   ┌─────────────────────────┐
    │  RecordingService          │   │  TranscriptionService   │
    │  • Audio capture           │   │  • Klangio API          │
    │  • Cloud storage           │   │  • Replicate API        │
    │  • Quality analysis        │   │  • MIDI processing      │
    └────────────────────────────┘   └─────────────────────────┘
                    │                                │
                    └────────────┬───────────────────┘
                                 ▼
                    ┌────────────────────────┐
                    │  UnifiedStudioStore    │
                    │  (Zustand)             │
                    │                        │
                    │  • recordingState      │
                    │  • transcriptionState  │
                    │  • takes               │
                    │  • chords              │
                    │  • midi                │
                    └────────────────────────┘
```

---

## 🔄 Workflow диаграммы

### 1. Recording Workflow

```
START
  │
  ├─ Выбрать источник
  │   ├─ Микрофон
  │   ├─ Облачный файл
  │   └─ Стем трека
  │
  ├─ Настроить мониторинг
  │   ├─ Включить метроном (BPM, размер)
  │   ├─ Включить тюнер (для гитары)
  │   └─ Настроить уровень
  │
  ├─ Записать дубль
  │   ├─ Count-in (2-4 такта)
  │   ├─ Запись (до 10 минут)
  │   └─ Auto-save в облако
  │
  ├─ Повторить для multi-take (опционально)
  │   └─ AI quality score для каждого
  │
  ├─ Сравнить дубли (A/B)
  │   └─ Выбрать лучший ИЛИ создать composite
  │
  ├─ Сохранить в проект
  │
END → Готов для transcription
```

### 2. Transcription Workflow

```
START (есть audio)
  │
  ├─ Выбрать движок
  │   ├─ Klangio (точнее, больше форматов)
  │   └─ Replicate (быстрее, универсальный)
  │
  ├─ Выбрать модель (для Klangio)
  │   ├─ guitar / piano / bass / drums / vocal
  │   └─ universal (auto-detect)
  │
  ├─ Запустить transcription
  │   └─ Progress: 0% → 100% (10-60 сек)
  │
  ├─ Получить результаты
  │   ├─ MIDI data
  │   ├─ Chord timeline
  │   ├─ Beat markers
  │   └─ Notes/tabs
  │
  ├─ Визуализация
  │   ├─ ChordTimeline (overlay на waveform)
  │   ├─ MIDIRollViewer (piano roll / guitar tab)
  │   └─ Interactive editing
  │
  ├─ Экспорт
  │   ├─ MIDI (.mid, .mid quantized)
  │   ├─ Notation (MusicXML, GP5, PDF)
  │   └─ Chords (ChordPro, text)
  │
END
```

### 3. Add Vocal Workflow

```
START (есть instrumental трек)
  │
  ├─ Открыть "Add Vocal"
  │
  ├─ Записать вокал
  │   ├─ Использовать RecordingPanel
  │   ├─ Метроном с BPM трека
  │   └─ Multi-take поддержка
  │
  ├─ Настроить эффекты
  │   ├─ Reverb (0-100%)
  │   ├─ Delay (0-100%)
  │   ├─ Compression
  │   └─ Pitch correction (auto-tune)
  │
  ├─ Настроить баланс
  │   ├─ Vocal volume
  │   ├─ Instrumental volume
  │   └─ Preview mix
  │
  ├─ Отправить на генерацию
  │   └─ Suno AI v5 (extend_audio)
  │
  ├─ Дождаться результата (30-120 сек)
  │
END → Новый трек с вокалом
```

### 4. Chord Detection Workflow

```
START (есть audio)
  │
  ├─ Запустить chord detection
  │   └─ Klangio API (chord-recognition-extended)
  │
  ├─ Получить chord timeline
  │   └─ Array of {chord, start, end, confidence}
  │
  ├─ Визуализация на waveform
  │   ├─ ChordTimeline (overlay)
  │   └─ Sync с playback
  │
  ├─ Click на аккорд
  │   ├─ Показать ChordDiagram
  │   ├─ Guitar fingering
  │   └─ Alternative positions
  │
  ├─ Редактирование (опционально)
  │   ├─ Изменить аккорд
  │   ├─ Изменить границы
  │   └─ Добавить/удалить
  │
  ├─ Экспорт
  │   ├─ ChordPro format
  │   ├─ Text with chords
  │   └─ PDF chart
  │
END
```

---

## 📊 Data Flow диаграмма

```
┌──────────────────────────────────────────────────────────────┐
│                    User Interaction                           │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                  React Components                             │
│  UnifiedRecordingStudio | TranscriptionWorkflow              │
└──────────────────────────────────────────────────────────────┘
                           │
                ┌──────────┼──────────┐
                ▼          ▼          ▼
        ┌───────────┐ ┌──────────┐ ┌────────────┐
        │  Hooks    │ │  Store   │ │  Services  │
        │           │ │          │ │            │
        │ useRecord │ │ Zustand  │ │ Recording  │
        │ Workflow  │ │ Store    │ │ Service    │
        │           │ │          │ │            │
        │ useTransc │ │ recording│ │ Transcript │
        │ ription   │ │ State    │ │ Service    │
        └───────────┘ └──────────┘ └────────────┘
                ▲          ▲          │
                └──────────┼──────────┘
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                    Edge Functions                             │
│  klangio-analyze | replicate-midi-transcription              │
└──────────────────────────────────────────────────────────────┘
                           │
                ┌──────────┼──────────┐
                ▼          ▼          ▼
        ┌───────────┐ ┌──────────┐ ┌────────────┐
        │ Klangio   │ │Replicate │ │  Suno AI   │
        │   API     │ │   API    │ │   v5 API   │
        └───────────┘ └──────────┘ └────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      Database                                 │
│  audio_takes | transcription_jobs | chord_data               │
│  studio_projects | track_stems                               │
└──────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                   Cloud Storage                               │
│  audio-takes bucket | reference-audio bucket                 │
└──────────────────────────────────────────────────────────────┘
```

---

## 🗂️ Database Schema

### Новые таблицы

```sql
┌─────────────────────────────────────────────────────────────┐
│  audio_takes                                                 │
├─────────────────────────────────────────────────────────────┤
│  id                UUID PRIMARY KEY                          │
│  project_id        UUID → studio_projects                    │
│  user_id           UUID → profiles                           │
│  audio_url         TEXT                                      │
│  duration_seconds  INTEGER                                   │
│  source            TEXT ('microphone' | 'cloud' | 'stem')    │
│  stem_id           UUID → track_stems                        │
│  metadata          JSONB                                     │
│  quality_score     DECIMAL(3,2)                              │
│  is_selected       BOOLEAN                                   │
│  created_at        TIMESTAMPTZ                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  transcription_jobs                                          │
├─────────────────────────────────────────────────────────────┤
│  id                UUID PRIMARY KEY                          │
│  audio_url         TEXT                                      │
│  engine            TEXT ('klangio' | 'replicate')            │
│  model             TEXT                                      │
│  status            TEXT ('pending' | 'processing' |          │
│                         'completed' | 'failed')              │
│  progress          INTEGER (0-100)                           │
│  results           JSONB                                     │
│  error             TEXT                                      │
│  created_at        TIMESTAMPTZ                               │
│  completed_at      TIMESTAMPTZ                               │
│  user_id           UUID → profiles                           │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  chord_data                                                  │
├─────────────────────────────────────────────────────────────┤
│  id                UUID PRIMARY KEY                          │
│  track_id          UUID → tracks                             │
│  stem_id           UUID → track_stems                        │
│  chords            JSONB (Array of Chord objects)            │
│  key               TEXT                                      │
│  detected_at       TIMESTAMPTZ                               │
│  source            TEXT ('klangio' | 'manual')               │
└─────────────────────────────────────────────────────────────┘
```

### Обновление существующих таблиц

```sql
ALTER TABLE studio_projects
ADD COLUMN recording_settings JSONB DEFAULT '{
  "metronome_enabled": false,
  "metronome_bpm": 120,
  "metronome_time_signature": "4/4",
  "count_in_bars": 2,
  "monitor_enabled": true
}'::jsonb;

ALTER TABLE track_stems
ADD COLUMN midi_url TEXT,
ADD COLUMN midi_quant_url TEXT,
ADD COLUMN musicxml_url TEXT,
ADD COLUMN gp5_url TEXT,
ADD COLUMN pdf_url TEXT,
ADD COLUMN transcription_metadata JSONB DEFAULT '{}'::jsonb;
```

---

## 🎯 Component Hierarchy

```
UnifiedStudioPage
└── StudioShell
    └── Recording Tab (NEW)
        │
        ├── UnifiedRecordingStudio
        │   │
        │   ├── RecordingPanel
        │   │   ├── SourceSelector
        │   │   │   ├── MicrophoneSource
        │   │   │   ├── CloudSource
        │   │   │   └── TrackSource
        │   │   │
        │   │   ├── RecordButton
        │   │   ├── RecordingControls
        │   │   └── TakesList
        │   │
        │   ├── MonitoringTools
        │   │   ├── Metronome (✅ exists)
        │   │   ├── GuitarTuner (✅ exists)
        │   │   ├── AudioLevelMeter (✅ exists)
        │   │   └── PitchMonitor (NEW)
        │   │
        │   └── TakeManager (NEW)
        │       ├── TakeItem (x N)
        │       ├── ABComparison
        │       └── CompositeMode
        │
        └── TranscriptionWorkflow
            │
            ├── TranscriptionPanel
            │   ├── EngineSelector
            │   ├── ModelSelector
            │   ├── TranscriptionProgress
            │   └── TranscriptionResults
            │
            ├── ChordVisualization (NEW)
            │   ├── ChordTimeline
            │   ├── ChordDiagram
            │   └── ChordLibrary
            │
            ├── MIDIRollViewer (NEW)
            │   ├── PianoRoll
            │   ├── DrumRoll
            │   └── GuitarTab
            │
            └── ExportOptionsPanel
                ├── FormatSelector
                ├── ExportPresets
                └── BatchExport
```

---

## 📦 Module Dependencies

```
┌────────────────────────────────────────────────────────────┐
│                      Application Layer                      │
├────────────────────────────────────────────────────────────┤
│  Pages: UnifiedStudioPage                                  │
│  Components: UnifiedRecordingStudio, TranscriptionWorkflow │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                     Presentation Layer                      │
├────────────────────────────────────────────────────────────┤
│  UI Components: RecordingPanel, ChordVisualization, etc.   │
│  Hooks: useRecordingWorkflow, useTranscriptionWorkflow     │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                       Business Layer                        │
├────────────────────────────────────────────────────────────┤
│  Services: RecordingService, TranscriptionService          │
│  State: useUnifiedStudioStore (Zustand)                    │
└────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌────────────────────────────────────────────────────────────┐
│                        Data Layer                           │
├────────────────────────────────────────────────────────────┤
│  API Clients: Klangio, Replicate, Suno                     │
│  Database: Supabase client                                 │
│  Storage: Cloud storage (audio-takes bucket)               │
└────────────────────────────────────────────────────────────┘
```

---

## 🔗 API Integration Flow

### Klangio API

```
Client Request
     │
     ▼
Edge Function: klangio-analyze
     │
     ├─ Prepare request
     │   ├─ Smart model selection (based on stem_type)
     │   └─ Smart outputs selection
     │
     ├─ POST to Klangio API
     │   └─ https://api.klangio.com/v1/jobs
     │
     ├─ Poll for completion
     │   └─ GET /v1/jobs/{jobId}
     │
     ├─ Download outputs
     │   ├─ MIDI file
     │   ├─ MusicXML file
     │   ├─ GP5 file (if available)
     │   ├─ PDF file (if available)
     │   └─ JSON metadata
     │
     ├─ Upload to storage
     │   └─ reference-audio bucket
     │
     ├─ Update database
     │   └─ transcription_jobs table
     │
     └─ Return to client
         └─ URLs + metadata
```

### Suno AI v5 - Add Vocal

```
Client Request (AddVocalDialog)
     │
     ├─ Upload vocal audio to storage
     │   └─ audio-takes bucket
     │
     ▼
Edge Function: suno-generate
     │
     ├─ Prepare request
     │   ├─ audio_url: instrumental stem URL
     │   ├─ vocals_audio_url: recorded vocal URL
     │   ├─ prompt: style description
     │   └─ continue_at: 0
     │
     ├─ POST to Suno API
     │   └─ /api/v5/extend_audio
     │
     ├─ Create generation_tasks record
     │   └─ status: 'pending'
     │
     ├─ Poll for completion
     │   └─ Webhook callback OR polling
     │
     ├─ Download result
     │   └─ New track with vocal + instrumental
     │
     ├─ Create track_versions
     │   └─ A/B versions
     │
     └─ Send notification
         └─ Telegram message
```

---

## 📈 Performance Considerations

### Optimization Strategy

```
┌────────────────────────────────────────────────────────────┐
│                    Loading Strategy                         │
├────────────────────────────────────────────────────────────┤
│  1. Initial Load                                           │
│     ├─ Core components (RecordingPanel)                    │
│     └─ Essential hooks (useRecordingWorkflow)              │
│                                                            │
│  2. Lazy Load on Demand                                    │
│     ├─ ChordVisualization (when chord detection runs)      │
│     ├─ MIDIRollViewer (when MIDI transcription completes)  │
│     └─ ExportOptionsPanel (when user clicks export)        │
│                                                            │
│  3. Code Splitting                                         │
│     ├─ Route-based (UnifiedStudioPage)                     │
│     └─ Component-based (heavy visualizations)              │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                    Caching Strategy                         │
├────────────────────────────────────────────────────────────┤
│  TanStack Query Config:                                    │
│  {                                                         │
│    staleTime: 5 * 60 * 1000,      // 5 minutes           │
│    gcTime: 30 * 60 * 1000,        // 30 minutes          │
│    refetchOnWindowFocus: false,                           │
│  }                                                         │
│                                                            │
│  Audio Caching:                                            │
│  • Decoded audio in IndexedDB                             │
│  • Waveform data in memory (LRU cache)                    │
│  • Chord data in Zustand store                            │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                  Web Workers Usage                          │
├────────────────────────────────────────────────────────────┤
│  • Audio decoding (WAV/MP3 → AudioBuffer)                  │
│  • Waveform generation (peaks extraction)                  │
│  • MIDI processing (note quantization)                     │
│  • Chord progression analysis                              │
└────────────────────────────────────────────────────────────┘
```

---

## 🎨 UI/UX Guidelines

### Mobile-First Design Principles

```
┌────────────────────────────────────────────────────────────┐
│                   Touch Targets                             │
├────────────────────────────────────────────────────────────┤
│  Minimum size: 56x56px (iOS guidelines)                    │
│  Spacing: 8px between interactive elements                 │
│  Safe areas: Respect Telegram safe areas                   │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                      Gestures                               │
├────────────────────────────────────────────────────────────┤
│  • Swipe left/right - Switch between takes                 │
│  • Pinch to zoom - Zoom on waveform                        │
│  • Long press - Open context menu                          │
│  • Pull down - Refresh/reload                              │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│                  Haptic Feedback                            │
├────────────────────────────────────────────────────────────┤
│  • Light impact - Button tap                               │
│  • Medium impact - Recording start/stop                    │
│  • Heavy impact - Generation complete                      │
│  • Success/Error - Notification haptic                     │
└────────────────────────────────────────────────────────────┘
```

### Visual Hierarchy

```
┌────────────────────────────────────────────────────────────┐
│  Recording Button (Primary Action)                         │
│  ┌──────────────────────────────────────────────────────┐ │
│  │         ⏺️  Tap to Record                            │ │
│  └──────────────────────────────────────────────────────┘ │
│                                                            │
│  Monitoring Tools (Secondary)                              │
│  ┌────────┐ ┌────────┐ ┌────────┐                        │
│  │Metronome│ │ Tuner  │ │ Level  │                        │
│  └────────┘ └────────┘ └────────┘                        │
│                                                            │
│  Takes List (Tertiary)                                     │
│  ┌──────────────────────────────────────────────────────┐ │
│  │ Take 1  [waveform] 2:34 ⭐ 92                         │ │
│  │ Take 2  [waveform] 2:31 ⭐ 88                         │ │
│  │ Take 3  [waveform] 2:36 ⭐ 95 ✓                       │ │
│  └──────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

---

## 🚀 Ready for Implementation

Эта визуальная документация дополняет основной план и помогает понять:
- Архитектуру системы
- Взаимодействие компонентов
- Потоки данных
- Интеграции с API
- Структуру базы данных

**Следующий шаг:** Начать реализацию Phase 1 (Foundation & Fixes)

**Полная документация:** [STUDIO_INTEGRATION_PLAN_2026.md](./STUDIO_INTEGRATION_PLAN_2026.md)
