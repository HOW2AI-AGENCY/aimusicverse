# Аудит прогресса — 2026-07-04

Полная сверка заявленного состояния проекта (PROJECT_STATUS.md, ROADMAP.md) с фактическим состоянием кодовой базы, CI на `main` и историей коммитов. Проведён после закрытия Sprint 049 и серии прямых хотфикс-коммитов в `main` от 2026-07-04.

> **TL;DR:** метрики кода в документации честные (0 `any`, 9 файлов >800 строк, спринты 042–049 реально завершены), но `main` на момент аудита **красный** — прямые коммиты из Lovable внесли Prettier-дрейф в 24 файла, workflow «Docs» падает на link-check, вердикт по E2E ещё не получен. Документация не покрывает хотфикс-миграции 2026-07-04 (per-version лайки фактически доехали до прод другим путём, чем описано).

## 1. Сверка метрик: заявлено vs проверено

| Метрика                             | Заявлено (PROJECT_STATUS.md) | Проверено                                                    |      Статус      |
| ----------------------------------- | ---------------------------- | ------------------------------------------------------------ | :--------------: |
| `any` в `src/` (бюджет ≤50)         | 0                            | **0** (`node scripts/count-any.mjs` — прогнан локально)      |        ✅        |
| Файлов >800 строк                   | 9                            | **9** (без generated `types.ts` и data-файла `drum-kits.ts`) |        ✅        |
| Список 9 файлов >800 LOC            | актуализирован 2026-07-04    | совпадает пофайлово с фактическим `wc -l`                    |        ✅        |
| Спринты 042–049 завершены           | да                           | коммиты существуют, история сходится                         |        ✅        |
| Prettier на `main`                  | (не отражено)                | **24 файла с дрейфом** — Format check в CI падает            |        ❌        |
| E2E зелёный в CI                    | «не подтверждён»             | прогон run #115 (коммит `32375fe`, #572) — in progress       |        🟡        |
| Миграция per-version лайков на прод | «не применена, ждёт деплой»  | применена **другой** миграцией `20260704014859` (см. §3)     | ❌ (док устарел) |

Проверка `tsc --noEmit` в среде аудита не показательна (в контейнере не установлены зависимости — ошибки «Cannot find module 'react'»); ориентир — workflow «Quality & Build», который на двух последних коммитах `main` (`1836c9f`, `68c2867`) **зелёный**.

## 2. Состояние CI на `main` (по workflow, на 2026-07-04 ~06:30 UTC)

| Workflow                   | Последний вердикт                      | Причина падения                                                                                                                                                                              |
| -------------------------- | -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CI/CD Pipeline**         | ❌ failure (все прогоны 2026-07-03/04) | Шаг **Format check**: 24 файла не проходят Prettier (внесены прямыми коммитами, см. §3). Все последующие джобы (TypeScript, Unit tests, Build & Size, E2E, Smoke) — skipped.                 |
| **Docs**                   | ❌ failure (стабильно)                 | Шаг **Check links (lychee)** — битые ссылки в markdown. Markdown lint при этом зелёный.                                                                                                      |
| **E2E Tests (Playwright)** | 🟡 in progress (run #115 по #572)      | Предыдущие прогоны падали на сборе тестов: `a11y.axe.spec.ts` импортировал недекларированный `@axe-core/playwright` (исправлено в #572); вердикт нового прогона на момент аудита не получен. |
| **Quality & Build**        | ✅ success (2 последних коммита)       | —                                                                                                                                                                                            |
| **Update README badges**   | ❌ failure                             | Вторично: падает вместе с остальными на тех же коммитах.                                                                                                                                     |

**Вывод:** единственный реальный блокер зелёного `main` прямо сейчас — Prettier-дрейф (устранён в этой ветке, см. §5). Docs/lychee — независимая, менее критичная проблема. E2E — ждём run #115.

## 3. Хронология прямых коммитов 2026-07-04 (Lovable) и их последствия

2026-07-04 в 01:00–02:00 UTC в `main` влиты ~10 прямых коммитов из Lovable («Changes», «Work in progress», «Исправил лайки и версий», «Убрал несуществующую колонку», «Fixed security issues») **в обход pre-commit хуков** (prettier/eslint/tsc/commitlint не прогонялись).

**Содержательно это ценные фиксы:**

- `a5ddfdc` «Исправил лайки и версий» + миграция `20260704014859`: per-version лайки доведены до рабочего состояния прямо на прод — `track_likes.track_version_id` NOT NULL + backfill + уникальность `(user_id, track_id, track_version_id)`; правки `src/api/tracks.api.ts`, `src/hooks/usePublicTracks.ts`; перегенерирован `src/integrations/supabase/types.ts`.
- `48ae635` «Убрал несуществующую колонку» + миграция `20260704015457`: починен триггер `update_track_likes_count` (ссылался на несуществующую колонку) — теперь инкрементит `tracks.likes_count` **и** `track_versions.likes_count`.
- `68c2867` «Fixed security issues» + миграция `20260704015640`: security-hardening RLS — убрана дублирующая admin-политика на `api_usage_logs`, запрет обхода модерации комментариев через BEFORE UPDATE триггер, storage-политики `reference-audio` переведены с `public` на `authenticated`.

**Побочные эффекты (техдолг от прямых коммитов):**

1. **Prettier-дрейф в 24 файлах** → красный Format check на каждый пуш в `main` (см. §2). Устранён в этой ветке.
2. **Build-артефакты в git:** `tsconfig.app.tsbuildinfo`, `tsconfig.node.tsbuildinfo` закоммичены в `a5ddfdc`. Убраны из индекса в этой ветке, `*.tsbuildinfo` добавлен в `.gitignore`.
3. **`bun.lock`/`bun.lockb` рядом с `package-lock.json`** — два пакетных менеджера в репозитории; Lovable коммитит bun-lock, CI использует npm. Источник расхождений зависимостей. Не трогаем в этой ветке (может сломать Lovable-flow) — вынесено в план Sprint 050 как процессный пункт.
4. **Две пересекающиеся миграции per-version лайков в репозитории:** `20260703120000_per_version_track_likes.sql` (Sprint 049, на прод не применялась) и `20260704014859_…` (Lovable, применена на прод). Обе идемпотентно-написаны (`IF NOT EXISTS`/`DROP IF EXISTS`), но при накатке на чистое окружение выполнятся **обе** — порядок и совместимость нужно верифицировать. ⚠️ Прямую сверку списка применённых миграций на прод-БД в рамках аудита выполнить не удалось (Supabase MCP требует ручного approval) — **требует подтверждения**.

## 4. Документация: что устарело

- PROJECT_STATUS.md (раздел Sprint 049): «⚠️ Миграция не применена на прод БД — ждёт деплой-пайплайна» — устарело: функциональность доехала до прод миграцией `20260704014859` (см. §3).
- Секция «Недавно смержено» PROJECT_STATUS.md не покрывает прямые коммиты 2026-07-04.
- «Активные блокеры» не содержат Format check и Docs/lychee.

Исправляется в этой же ветке (см. §5).

## 5. Что сделано в рамках аудита (ветка `claude/progress-audit-plan-fpobal`)

1. **Prettier-дрейф устранён** — `prettier --write` по 24 файлам, семантических изменений нет; `npx prettier --check "src/**/*.{ts,tsx}"` — 0 предупреждений. `tsbuildinfo` убраны из git.
2. **Этот отчёт** — `docs/audit/PROGRESS-AUDIT-2026-07-04.md`.
3. **Актуализация PROJECT_STATUS.md / ROADMAP.md** + план **Sprint 050** (`SPRINTS/SPRINT-050-PLAN.md`).

## 6. Риски и рекомендации

| Риск                                                              | Критичность | Рекомендация                                                                                                    |
| ----------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------- |
| Прямые коммиты в `main` мимо хуков будут повторяться              | 🔴 High     | Либо auto-format workflow на push в `main`, либо branch protection + работа Lovable через ветки (Sprint 050-F). |
| Статус применённых миграций на прод не верифицирован              | 🟠 Medium   | Разовая сверка `supabase migration list` / MCP `list_migrations` с ручным approval (Sprint 050-A).              |
| Две пересекающиеся likes-миграции при накатке на чистое окружение | 🟠 Medium   | Проверить последовательное выполнение обеих на локальном Supabase (Sprint 050-A).                               |
| E2E-вердикт неизвестен                                            | 🟠 Medium   | Дождаться run #115; если красный — чинить по фактическим падениям тестов, сбор suite уже исправлен (#572).      |
| Тест-покрытие 282 юнит-теста при 1003 компонентах                 | 🟠 Medium   | Sprint 051 (tests-first декомпозиция) — без изменений.                                                          |

## 7. Итоговая оценка

Кодовая база в хорошей форме — заявленные в документации инженерные метрики подтверждаются фактически, динамика спринтов 042–049 реальна. Главная системная проблема не в коде, а в **процессе**: два канала доставки в `main` (PR с хуками и CI vs прямые Lovable-коммиты без них) регулярно оставляют `main` красным и документацию устаревшей. План работ: [ROADMAP.md → Sprint 050–052](../../ROADMAP.md#-sprint-history), детально: [SPRINTS/SPRINT-050-PLAN.md](../../SPRINTS/archived/SPRINT-050-PLAN.md).
