# Улучшения интерфейса помощника по лирике с учетом контекста проекта

## Проблема

До внедрения изменений наблюдались следующие проблемы:

1. **Отсутствие контекста проекта**: AI Lyrics Assistant не получал информацию о проекте при открытии из треклиста проекта
2. **Потеря данных**: Существующая лирика из заметок трека не передавалась в помощник
3. **Нет визуальной индикации**: Пользователь не видел, что работает в контексте конкретного проекта/трека
4. **Дублирование ввода**: Пользователю приходилось повторно вводить жанр, настроение, которые уже заданы в проекте
5. **Неудобная навигация**: Не было специальных опций для работы с существующей лирикой

## Решение

### 1. Новый тип данных `ProjectContext`

Создан интерфейс для передачи полного контекста проекта и трека:

```typescript
export interface ProjectContext {
  projectId: string;
  projectName?: string;
  projectGenre?: string | null;
  projectMood?: string | null;
  projectLanguage?: string | null;
  trackId?: string;
  trackTitle?: string;
  trackNotes?: string | null;
  stylePrompt?: string | null;
}
```

**Файл**: `src/components/generate-form/lyrics-chat/types.ts`

### 2. Визуальная индикация контекста проекта

Добавлен баннер сверху AI Assistant, показывающий:
- 📁 Название проекта
- 🎵 Название трека
- Жанр и настроение (в виде тегов)

```tsx
{projectContext && (
  <div className="px-4 py-2 bg-primary/5 border-b border-primary/10">
    <div className="flex items-center gap-2 text-xs">
      {projectContext.projectName && (
        <span className="font-medium text-primary">
          📁 {projectContext.projectName}
        </span>
      )}
      {projectContext.trackTitle && (
        <span className="font-medium">
          🎵 {projectContext.trackTitle}
        </span>
      )}
    </div>
    {/* Genre and mood badges */}
  </div>
)}
```

**Файл**: `src/components/generate-form/LyricsChatAssistant.tsx`

### 3. Умный старт диалога с учетом контекста

Диалог начинается по-разному в зависимости от контекста:

#### A. С контекстом проекта и треком
```
Привет! 👋 Я помогу написать лирику для трека "Track Name" из проекта "Project Name".
```

#### B. Если есть существующая лирика в заметках
```
У вас уже есть черновик лирики. Хотите:
• ✨ Улучшить текущий
• 🆕 Создать новый
• ➕ Продолжить текущий
```

#### C. Если жанр и настроение уже заданы в проекте
Пропускает шаги выбора жанра и настроения, сразу предлагает:
```
📝 Жанр: Rock, настроение: энергичное. О чём будет песня?
```

**Файл**: `src/components/generate-form/lyrics-chat/useLyricsChat.ts`

### 4. Три режима работы с существующей лирикой

#### Режим "Улучшить текущий" (`improve_existing`)
- Загружает существующую лирику из заметок трека
- Показывает её в превью
- Предлагает внести изменения через чат

#### Режим "Продолжить текущий" (`continue_existing`)
- Берет существующий текст как базу
- Просит пользователя указать, что добавить
- Генерирует продолжение в том же стиле

#### Режим "Создать новый" (`create_new`)
- Начинает с чистого листа
- Но использует жанр/настроение из проекта
- Пропускает уже заполненные шаги

### 5. Интеграция с ProjectTracklistTab

`ProjectTracklistTab` теперь передает полные данные проекта в `LyricsPreviewSheet`:

```tsx
<LyricsPreviewSheet
  track={lyricsPreviewTrack}
  projectName={project.name}
  projectGenre={project.genre}
  projectMood={project.mood}
  projectLanguage={project.language}
  onSave={handleSaveLyrics}
/>
```

**Файл**: `src/components/project/ProjectTracklistTab.tsx`

### 6. Обновление LyricsPreviewSheet

`LyricsPreviewSheet` собирает полный контекст и передает в AI Assistant:

```tsx
const projectContext: ProjectContext = {
  projectId: track.project_id,
  projectName,
  projectGenre,
  projectMood,
  projectLanguage,
  trackId: track.id,
  trackTitle: track.title,
  trackNotes: track.notes,
  stylePrompt: track.style_prompt,
};

<LyricsChatAssistant
  projectContext={projectContext}
  initialGenre={projectGenre || undefined}
  initialMood={projectMood ? [projectMood] : undefined}
  initialLanguage={(projectLanguage as 'ru' | 'en') || 'ru'}
/>
```

**Файл**: `src/components/project/LyricsPreviewSheet.tsx`

## Сценарии использования

### Сценарий 1: Создание лирики для нового трека в проекте

1. Пользователь открывает проект "Альбом 2024"
2. Нажимает на кнопку "Редактировать лирику" у трека "Песня 1"
3. Нажимает "AI Помощник"
4. Видит баннер: "📁 Альбом 2024 → 🎵 Песня 1"
5. Система пропускает выбор жанра/настроения (уже есть в проекте)
6. Сразу предлагает: "О чём будет песня?"

### Сценарий 2: Улучшение существующей лирики

1. Пользователь открывает трек с черновиком лирики
2. Нажимает "AI Помощник"
3. Видит контекст проекта и опции:
   - ✨ Улучшить текущий
   - 🆕 Создать новый
   - ➕ Продолжить текущий
4. Выбирает "Улучшить текущий"
5. Видит текущую лирику в превью
6. Пишет: "Добавь больше метафор"
7. AI улучшает существующий текст

### Сценарий 3: Продолжение лирики

1. У трека есть куплет и припев в заметках
2. Пользователь открывает AI Помощник
3. Выбирает "Продолжить текущий"
4. Пишет: "Добавь второй куплет про преодоление трудностей"
5. AI генерирует продолжение в том же стиле

## Преимущества

### Для пользователя
- ✅ Меньше кликов и ввода данных
- ✅ Видит контекст работы
- ✅ Не теряет существующие наработки
- ✅ Гибкость в работе с лирикой (улучшить/продолжить/создать заново)

### Для системы
- ✅ Лучший контекст для AI = лучшие результаты
- ✅ Согласованность данных проекта и треков
- ✅ Меньше повторяющихся запросов
- ✅ Более релевантные генерации

## Технические детали

### Передача контекста через компоненты

```
ProjectTracklistTab
  ↓ project data
LyricsPreviewSheet
  ↓ projectContext
LyricsChatAssistant
  ↓ projectContext
useLyricsChat (hook)
  ↓ uses context for smart initialization
```

### Состояние в useLyricsChat

```typescript
const [theme, setTheme] = useState(initialTheme || '');
const [genre, setGenre] = useState(
  initialGenre || projectContext?.projectGenre || ''
);
const [mood, setMood] = useState<string[]>(
  initialMood || (projectContext?.projectMood ? [projectContext.projectMood] : [])
);
```

### API вызовы

AI Lyrics Assistant получает расширенный контекст:
- theme (из user input или notes)
- genre (из project или user choice)
- mood (из project или user choice)
- language (из project)
- structure (user choice)

## Будущие улучшения

1. **Автосохранение**: Сохранять промежуточные версии лирики
2. **История изменений**: Показывать что AI изменил в тексте
3. **Стилистический анализ**: Анализировать существующую лирику и копировать стиль
4. **Мультитрек генерация**: Генерировать лирику для всех треков проекта с учетом общей концепции
5. **Collaborative mode**: Работа нескольких пользователей над лирикой проекта

## Заключение

Интеграция контекста проекта в AI Lyrics Assistant делает работу над лирикой более эффективной и удобной. Пользователи получают персонализированный опыт с учетом их проекта, а AI получает больше информации для создания релевантных текстов.
