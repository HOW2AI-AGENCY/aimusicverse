DROP POLICY IF EXISTS "Users can upload their own reference audio" ON storage.objects;
DROP POLICY IF EXISTS "Users can view their own reference audio" ON storage.objects;
DROP POLICY IF EXISTS "Users can delete their own reference audio" ON storage.objects;