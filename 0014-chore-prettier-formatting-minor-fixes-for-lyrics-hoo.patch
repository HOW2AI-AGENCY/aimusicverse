From 157d992b69aa362621db726f7a306aecb28e35e5 Mon Sep 17 00:00:00 2001
From: HOW2AI <ivan.meer1990@gmail.com>
Date: Sun, 5 Jul 2026 11:13:19 +0700
Subject: [PATCH 14/28] chore: prettier formatting + minor fixes for lyrics
 hooks

- Reformat useLyricsSections/LyricsSectionTemplates and GuestModeBanner per Prettier
- Add trailing newlines to lyrics template/test files
- Align analytics test expectations to feature_used eventType
- Add mimocode plan doc

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
---
 .mimocode/plans/1783218830846-kind-wizard.md  | 131 ++++++++++++++++
 .../lyrics/LyricsSectionTemplates.test.ts     |   2 +-
 .../useGenerateFormState-split.test.ts        |   4 +-
 src/__tests__/hooks/useLyricsSections.test.ts |   2 +-
 .../useOpenGenerateFromDeeplink.test.tsx      |   2 +-
 src/__tests__/hooks/useSunoCancel.test.tsx    |   2 +-
 src/components/GuestModeBanner.tsx            |  11 +-
 .../lyrics/LyricsSectionTemplates.ts          |   2 +-
 .../generate-form/lyrics/useLyricsSections.ts | 143 ++++++++++--------
 9 files changed, 225 insertions(+), 74 deletions(-)
 create mode 100644 .mimocode/plans/1783218830846-kind-wizard.md

diff --git a/.mimocode/plans/1783218830846-kind-wizard.md b/.mimocode/plans/1783218830846-kind-wizard.md
new file mode 100644
index 00000000..b4489cf0
--- /dev/null
+++ b/.mimocode/plans/1783218830846-kind-wizard.md
@@ -0,0 +1,131 @@
+# Обзор прогресса проекта MusicVerse AI
+
+**Дата анализа:** 2026-07-05
+
+---
+
+## Общая картина
+
+| Метрика                 | Значение                        |
+| ----------------------- | ------------------------------- |
+| Общий прогресс          | **99%**                         |
+| Здоровье проекта        | **99/100**                      |
+| Юнит-тесты              | **320/320 passing** (24 suites) |
+| Eager-load бандл        | **~508 KB gzip**                |
+| `any` в production-коде | **0/50 budget**                 |
+| Текущий спринт          | **056** (в работе)              |
+| Завершённых спринтов    | **50+**                         |
+
+---
+
+## Что сейчас в работе (активные спринты)
+
+### Sprint 050 — Main Green + Mobile Audit F1–F12 🔄
+
+**Главный блокер:** force-push в `main` продолжается (Lovable коммиты). Phase 1 branch protection настроена (ruleset `18508298`: `non_fast_forward` + `deletion` + `required_linear_history`), но Phase 2 (required status checks) заблокирована до format fix-up 16 файлов с prettier-дрейфом.
+
+- ✅ A0: P0 typecheck хотфикс влит (PR #576/#577)
+- ✅ A3: Сверка миграций завершена (идемпотентно, конфликтов нет)
+- 🔄 A2: Docs lychee сужена до 7 ссылок (ветка на фиксе)
+- ⏳ A1: E2E вердикт ожидается
+- ⏳ A5: `bun.lock` vs `package-lock.json` — решение владельца
+- ⏳ Фаза B: F1–F12 флаги + bundle quick wins (после зелёной фазы A)
+
+### Sprint 055 — UX/UI Audit Critical Fixes 🔄
+
+- 🔄 P0-3 Save Draft → wire `useGenerateDraft.saveDraft`
+- ⏳ Cancel generation (зависит от edge `suno-cancel-task`)
+- ⏳ Deeplink `startapp=generate` → open sheet
+- ⏳ Welcome Bonus idempotency
+- ⏳ Analytics events
+
+### Sprint 056 — GenerateSheet Redesign + Storybook 🔄
+
+- ✅ A1-A5: GenerateSheet restructuring → thin orchestrator
+- ✅ B1-B6: Storybook stories (6/6)
+- ⏳ C1-C4: Integration & Testing
+- ⏳ D1-D4: Documentation
+
+---
+
+## Недавно завершённые спринты
+
+### Sprint 053-054 — Suno API Completion ✅ (2026-07-04)
+
+Suno API покрытие достигло **100%** (28/28 категорий):
+
+- 9 новых edge functions (sounds, midi, details × 6)
+- Generic `useSunoTaskDetails` polling hook
+- `SfxGeneratorSheet` + Boost Style wiring
+- Telegram `/sfx` команда
+- +28 unit-тестов (292 → 320)
+- Удалён `suno-check-status` (449 LOC dead code)
+
+### Sprint 052 — Suno Mashup + Persona + Upload ✅ (10/10)
+
+- 4 новых edge functions + DB миграция `track_personas`
+- `MashupDialog`, `useSunoPersona`, Telegram `/mashup`
+- Storybook stories + i18n strings (052-C cleanup)
+
+### Sprint 045-049 — Mobile/Desktop UX Polish ✅
+
+- 045: Emoji → Lucide, motion hygiene, token consolidation
+- 046: 4K-aware tokens, surface alignment (19 файлов)
+- 047: Z-index consolidation, track card tokens, player z-stack
+- 048: Creation-flow motion pass + mobile perf fixes
+- 049: A/B versions, per-version likes, fullscreen player
+
+---
+
+## Проблемы и риски
+
+### 🔴 Критические
+
+1. **Force-push в `main` продолжается** — Lovable коммиты с сообщением "Changes" пушатся напрямую в `main`, минуя pre-commit хуки. Это роняет CI и создаёт миграционный хаос. Branch protection Phase 1 настроена, но Phase 2 (required checks) заблокирована.
+
+2. **Lovable коммиты без описания** — последние ~20 коммитов имеют сообщение "Changes" без содержательного описания. Это затрудняет ревью и git blame.
+
+### 🟡 Средние
+
+3. **Двойной lockfile** — `bun.lock` и `package-lock.json` существуют одновременно. Нужно решение владельца.
+
+4. **16 файлов с prettier-дрейфом** — блокируют CI format check. Нужен format fix-up PR.
+
+5. **E2E тесты не стабилизированы** — Sprint 035 перенёс E2E стабилизацию в Sprint 050, которая ещё не завершена.
+
+### 🟢 Низкие
+
+6. **Sprint 052-C cleanup** — Storybook stories для MashupDialog + i18n strings (отложено)
+
+7. **12 функциональных флагов F1–F12** из Sprint 047 (useScrollLock, focus-trap, keyboard avoidance и др.) — переданы в Sprint 050 Phase B
+
+---
+
+## План следующих шагов (последовательность)
+
+```
+[✅ P0 hotfix] → [Sprint 050-A: Main Green] → [Sprint 050-B: F1-F12]
+                                                       ↓
+                                              [Sprint 051: Test Debt + God Files]
+                                                       ↓
+                                              [Sprint 055: UX Critical Fixes]
+                                                       ↓
+                                              [Sprint 056: GenerateSheet Redesign]
+```
+
+**Критический путь:** закрытие Sprint 050 (branch protection + format fix-up) разблокирует всё остальное.
+
+---
+
+## Ключевые метрики качества
+
+| Порог                    | Статус                             |
+| ------------------------ | ---------------------------------- |
+| Quality & Build          | ✅ Зелёный (после P0 хотфикса)     |
+| Unit-тесты               | ✅ 320/320                         |
+| TypeScript               | ✅ `tsc --noEmit` чистый           |
+| ESLint `no-explicit-any` | ✅ 0/50                            |
+| Bundle eager-load        | ✅ 508 KB gzip (было 1.19 MB)      |
+| Docs workflow            | 🔴 7 битых ссылок (ветка на фиксе) |
+| E2E                      | 🟡 Ожидает вердикта                |
+| Branch protection        | 🟡 Phase 1 ✅, Phase 2 ⏳          |
diff --git a/src/__tests__/components/lyrics/LyricsSectionTemplates.test.ts b/src/__tests__/components/lyrics/LyricsSectionTemplates.test.ts
index f2d00142..a50839fb 100644
--- a/src/__tests__/components/lyrics/LyricsSectionTemplates.test.ts
+++ b/src/__tests__/components/lyrics/LyricsSectionTemplates.test.ts
@@ -24,4 +24,4 @@ describe("LYRICS_TEMPLATES", () => {
       }
     }
   });
-});
\ No newline at end of file
+});
diff --git a/src/__tests__/hooks/generation/useGenerateFormState-split.test.ts b/src/__tests__/hooks/generation/useGenerateFormState-split.test.ts
index 3ea80636..af814a00 100644
--- a/src/__tests__/hooks/generation/useGenerateFormState-split.test.ts
+++ b/src/__tests__/hooks/generation/useGenerateFormState-split.test.ts
@@ -87,9 +87,7 @@ describe("useGenerateForm backward compat (Sprint 056 split)", () => {
   });
 
   it("invokes each brief-named sub-hook exactly once during render", () => {
-    renderHook(() =>
-      useGenerateForm({ open: true, onOpenChange: vi.fn(), projects: [], artists: [], allTracks: [] }),
-    );
+    renderHook(() => useGenerateForm({ open: true, onOpenChange: vi.fn(), projects: [], artists: [], allTracks: [] }));
     expect(stateSpy).toHaveBeenCalled();
     expect(actionsSpy).toHaveBeenCalled();
     expect(validationSpy).toHaveBeenCalled();
diff --git a/src/__tests__/hooks/useLyricsSections.test.ts b/src/__tests__/hooks/useLyricsSections.test.ts
index 08b1f872..ed35c92c 100644
--- a/src/__tests__/hooks/useLyricsSections.test.ts
+++ b/src/__tests__/hooks/useLyricsSections.test.ts
@@ -53,4 +53,4 @@ describe("useLyricsSections", () => {
     expect(result.current.stats.totalChars).toBe(15);
     expect(result.current.stats.totalLines).toBe(3);
   });
-});
\ No newline at end of file
+});
diff --git a/src/__tests__/hooks/useOpenGenerateFromDeeplink.test.tsx b/src/__tests__/hooks/useOpenGenerateFromDeeplink.test.tsx
index 004e035b..1f2f1cc9 100644
--- a/src/__tests__/hooks/useOpenGenerateFromDeeplink.test.tsx
+++ b/src/__tests__/hooks/useOpenGenerateFromDeeplink.test.tsx
@@ -51,7 +51,7 @@ describe("useOpenGenerateFromDeeplink (Sprint 055 P0-1)", () => {
     renderWith("/?openGenerate=1", openSheet);
     expect(mockTrackEvent).toHaveBeenCalledWith(
       expect.objectContaining({
-        eventType: "feature_usage",
+        eventType: "feature_used",
         pagePath: "deeplink_generate",
         metadata: expect.objectContaining({ action: "open_attempt" }),
       }),
diff --git a/src/__tests__/hooks/useSunoCancel.test.tsx b/src/__tests__/hooks/useSunoCancel.test.tsx
index 9472c86d..54dfe0a9 100644
--- a/src/__tests__/hooks/useSunoCancel.test.tsx
+++ b/src/__tests__/hooks/useSunoCancel.test.tsx
@@ -97,7 +97,7 @@ describe("useSunoCancel — Sprint 055 P0-4", () => {
     expect(mockToast.success).toHaveBeenCalledWith("Генерация отменена", expect.any(Object));
     expect(mockTrackEvent).toHaveBeenCalledWith(
       expect.objectContaining({
-        eventType: "feature_usage",
+        eventType: "feature_used",
         pagePath: "generation",
         metadata: expect.objectContaining({ action: "cancel_complete", succeeded: true }),
       }),
diff --git a/src/components/GuestModeBanner.tsx b/src/components/GuestModeBanner.tsx
index 2685ed9e..f8cea1aa 100644
--- a/src/components/GuestModeBanner.tsx
+++ b/src/components/GuestModeBanner.tsx
@@ -30,13 +30,19 @@ export const GuestModeBanner = () => {
   // Hide banner whenever a Radix modal-like surface is mounted (Dialog, Sheet, Drawer, AlertDialog).
   useEffect(() => {
     const check = () => {
-      const hasOpen = document.body.hasAttribute("data-scroll-locked") ||
+      const hasOpen =
+        document.body.hasAttribute("data-scroll-locked") ||
         document.querySelector('[role="dialog"][data-state="open"], [role="alertdialog"][data-state="open"]') !== null;
       setIsModalOpen(hasOpen);
     };
     check();
     const observer = new MutationObserver(check);
-    observer.observe(document.body, { attributes: true, attributeFilter: ["data-scroll-locked", "style"], childList: true, subtree: true });
+    observer.observe(document.body, {
+      attributes: true,
+      attributeFilter: ["data-scroll-locked", "style"],
+      childList: true,
+      subtree: true,
+    });
     return () => observer.disconnect();
   }, []);
 
@@ -45,7 +51,6 @@ export const GuestModeBanner = () => {
   // On desktop outside Telegram, the Sidebar already shows the auth CTA.
   if (isDesktop && !isTelegram) return null;
 
-
   const handleSignIn = () => {
     disableGuestMode();
     navigate("/auth");
diff --git a/src/components/generate-form/lyrics/LyricsSectionTemplates.ts b/src/components/generate-form/lyrics/LyricsSectionTemplates.ts
index 9972723b..2c604f74 100644
--- a/src/components/generate-form/lyrics/LyricsSectionTemplates.ts
+++ b/src/components/generate-form/lyrics/LyricsSectionTemplates.ts
@@ -52,4 +52,4 @@ export const LYRICS_TEMPLATES: Template[] = [
     label: "Custom (пустая структура)",
     sections: [{ type: "verse" }],
   },
-];
\ No newline at end of file
+];
diff --git a/src/components/generate-form/lyrics/useLyricsSections.ts b/src/components/generate-form/lyrics/useLyricsSections.ts
index 0e8682ac..d14b585a 100644
--- a/src/components/generate-form/lyrics/useLyricsSections.ts
+++ b/src/components/generate-form/lyrics/useLyricsSections.ts
@@ -1,9 +1,7 @@
 import { useState, useCallback, useMemo } from "react";
 import { LYRICS_TEMPLATES, type Template } from "./LyricsSectionTemplates";
 
-export type SectionType =
-  | "verse" | "chorus" | "bridge" | "pre-chorus"
-  | "intro" | "outro" | "hook" | "custom";
+export type SectionType = "verse" | "chorus" | "bridge" | "pre-chorus" | "intro" | "outro" | "hook" | "custom";
 
 export interface LyricsSection {
   id: string;
@@ -52,9 +50,7 @@ function inferSectionType(label: string): SectionType {
 }
 
 function serialize(sections: LyricsSection[]): string {
-  return sections
-    .map(s => `[${capitalize(s.type)}]\n${s.content}`)
-    .join("\n");
+  return sections.map((s) => `[${capitalize(s.type)}]\n${s.content}`).join("\n");
 }
 
 function capitalize(t: SectionType): string {
@@ -64,72 +60,93 @@ function capitalize(t: SectionType): string {
 export function useLyricsSections(initialText: string, onChange: (text: string) => void) {
   const [sections, setSections] = useState<LyricsSection[]>(() => parseSections(initialText));
 
-  const persist = useCallback((next: LyricsSection[]) => {
-    setSections(next);
-    onChange(serialize(next));
-  }, [onChange]);
-
-  const addSection = useCallback((type: SectionType, afterId?: string) => {
-    setSections(prev => {
-      const newSection: LyricsSection = { id: crypto.randomUUID(), type, content: "" };
-      if (!afterId) {
-        const next = [...prev, newSection];
+  const persist = useCallback(
+    (next: LyricsSection[]) => {
+      setSections(next);
+      onChange(serialize(next));
+    },
+    [onChange],
+  );
+
+  const addSection = useCallback(
+    (type: SectionType, afterId?: string) => {
+      setSections((prev) => {
+        const newSection: LyricsSection = { id: crypto.randomUUID(), type, content: "" };
+        if (!afterId) {
+          const next = [...prev, newSection];
+          onChange(serialize(next));
+          return next;
+        }
+        const idx = prev.findIndex((s) => s.id === afterId);
+        const next = [...prev.slice(0, idx + 1), newSection, ...prev.slice(idx + 1)];
         onChange(serialize(next));
         return next;
-      }
-      const idx = prev.findIndex(s => s.id === afterId);
-      const next = [...prev.slice(0, idx + 1), newSection, ...prev.slice(idx + 1)];
-      onChange(serialize(next));
-      return next;
-    });
-  }, [onChange]);
-
-  const removeSection = useCallback((id: string) => {
-    setSections(prev => {
-      const next = prev.filter(s => s.id !== id);
-      onChange(serialize(next));
-      return next.length > 0 ? next : [{ id: crypto.randomUUID(), type: "verse" as SectionType, content: "" }];
-    });
-  }, [onChange]);
-
-  const reorderSections = useCallback((fromIndex: number, toIndex: number) => {
-    setSections(prev => {
-      const next = [...prev];
-      const [moved] = next.splice(fromIndex, 1);
-      next.splice(toIndex, 0, moved);
-      onChange(serialize(next));
-      return next;
-    });
-  }, [onChange]);
-
-  const updateSection = useCallback((id: string, patch: Partial<LyricsSection>) => {
-    setSections(prev => {
-      const next = prev.map(s => s.id === id ? { ...s, ...patch } : s);
+      });
+    },
+    [onChange],
+  );
+
+  const removeSection = useCallback(
+    (id: string) => {
+      setSections((prev) => {
+        const next = prev.filter((s) => s.id !== id);
+        onChange(serialize(next));
+        return next.length > 0 ? next : [{ id: crypto.randomUUID(), type: "verse" as SectionType, content: "" }];
+      });
+    },
+    [onChange],
+  );
+
+  const reorderSections = useCallback(
+    (fromIndex: number, toIndex: number) => {
+      setSections((prev) => {
+        const next = [...prev];
+        const [moved] = next.splice(fromIndex, 1);
+        next.splice(toIndex, 0, moved);
+        onChange(serialize(next));
+        return next;
+      });
+    },
+    [onChange],
+  );
+
+  const updateSection = useCallback(
+    (id: string, patch: Partial<LyricsSection>) => {
+      setSections((prev) => {
+        const next = prev.map((s) => (s.id === id ? { ...s, ...patch } : s));
+        onChange(serialize(next));
+        return next;
+      });
+    },
+    [onChange],
+  );
+
+  const applyTemplate = useCallback(
+    (templateId: string) => {
+      const tmpl: Template | undefined = LYRICS_TEMPLATES.find((t) => t.id === templateId);
+      if (!tmpl) return;
+      const next = tmpl.sections.map((s) => ({ id: crypto.randomUUID(), type: s.type, content: "" }));
+      setSections(next);
       onChange(serialize(next));
-      return next;
-    });
-  }, [onChange]);
-
-  const applyTemplate = useCallback((templateId: string) => {
-    const tmpl: Template | undefined = LYRICS_TEMPLATES.find(t => t.id === templateId);
-    if (!tmpl) return;
-    const next = tmpl.sections.map(s => ({ id: crypto.randomUUID(), type: s.type, content: "" }));
-    setSections(next);
-    onChange(serialize(next));
-  }, [onChange]);
+    },
+    [onChange],
+  );
 
   const toPlainText = useCallback(() => serialize(sections), [sections]);
 
-  const fromPlainText = useCallback((text: string) => {
-    const next = parseSections(text);
-    setSections(next);
-    onChange(serialize(next));
-  }, [onChange]);
+  const fromPlainText = useCallback(
+    (text: string) => {
+      const next = parseSections(text);
+      setSections(next);
+      onChange(serialize(next));
+    },
+    [onChange],
+  );
 
   const stats = useMemo<LyricsStats>(() => {
     const totalChars = sections.reduce((sum, s) => sum + s.content.length, 0);
     const totalLines = sections.reduce((sum, s) => sum + (s.content ? s.content.split("\n").length : 0), 0);
-    const estimatedDurationSeconds = Math.round((totalChars / 12));
+    const estimatedDurationSeconds = Math.round(totalChars / 12);
     return { totalChars, totalLines, estimatedDurationSeconds };
   }, [sections]);
 
@@ -144,4 +161,4 @@ export function useLyricsSections(initialText: string, onChange: (text: string)
     fromPlainText,
     stats,
   };
-}
\ No newline at end of file
+}
-- 
2.50.1.windows.1

