# План работ — 2026-07-14

**Ветка:** `claude/work-plan-task-status-7dfndr`
**Автор ревизии:** сессия обновления планов и статусов
**Метод верификации:** прямой `git diff`, рекурсивный `find`/`wc -l` по `src/` и `supabase/functions/`, `npm audit`, реальные прогоны `npm run typecheck:app` (**0 errors**) и `npm test` (**1810 passed**). Нюанс установки зависимостей — §5.

> **TL;DR.** Планирующие документы разошлись между собой (заявленные спринты 057↔064, тесты 1489↔1803). Этот документ сверяет реальное состояние с `git`/файловой системой, фиксирует фактически отгруженную работу как **Sprint 065 (Generate v2 + Home Redesign + Visual Regression)** и переопределяет ближайший бэклог (066–069).

---

## 0. Сверка расхождений (что было не так)

На момент ревизии живые документы противоречили друг другу и `git`:

| Документ                     | Заявленный спринт                     | Заявленные unit-тесты | Дата обновления |
| ---------------------------- | ------------------------------------- | --------------------- | --------------- |
| `PROJECT_STATUS.md` (бейдж)  | «Sprint 063 Start»                    | 1803                  | контент до 057  |
| `ROADMAP.md` (бейдж)         | 057 (in progress)                     | 1497                  | 2026-07-04      |
| `SPRINTS/SPRINT-PROGRESS.md` | 062 в работе, 063 план                | 1489                  | 2026-07-09      |
| `FUTURE_WORK_PLAN.md`        | 063 ✅ + 064 ✅                       | 1691                  | 2026-07-09      |
| **`git log` (факт)**         | **Generate v2 + Home — вне спринтов** | ⏳ (не проверено)     | 2026-07-13      |

**Решение по нумерации.** Наивысший «закрытый» спринт в трекерах — 064. Фактически отгруженная, но не задокументированная работа из последних коммитов получает номер **065**. Ранее набросанные в таймлайне от 2026-07-09 черновые «065 = тесты» / «066 = Sentry» **отменяются** и переносятся в 067/068 этого плана.

---

## 1. Проверенное состояние (2026-07-14)

| Метрика                                 | Значение                                                            | Источник                   | Статус       |
| --------------------------------------- | ------------------------------------------------------------------- | -------------------------- | ------------ |
| `*.service.ts`                          | 37                                                                  | `find src/services`        | ✅           |
| `*.api.ts`                              | 30                                                                  | `find src/api`             | ✅           |
| Хуки (`src/hooks/*.ts[x]`)              | 440                                                                 | `find src/hooks`           | ✅           |
| Компоненты (`src/components/*.tsx`)     | 1041                                                                | `find src/components`      | ✅           |
| Unit-тест файлы                         | 184                                                                 | `find src tests` (без e2e) | ✅           |
| E2E specs                               | 58                                                                  | `find tests/e2e`           | ✅           |
| Файлов >800 LOC в `src/`                | 0 (исключая `drum-kits.ts` — статические данные)                    | `wc -l` по `src/`          | ✅           |
| Файлов >800 LOC в `supabase/functions/` | ≥10 (до 1330 LOC)                                                   | `wc -l`                    | 🔴 tech-debt |
| `npm audit`                             | 4 (3 moderate, 1 low) на текущем дереве; сверить на чистом lockfile | `npm audit`                | 🟡           |
| Установка зависимостей                  | требует `--legacy-peer-deps` (vite 8 ↔ Storybook 8.6)               | `npm ci`                   | 🔴 tech-debt |
| Unit-тесты (проходят)                   | **1810 passed** (166 файлов, +2 skip-файла, 4 skip, 31 todo)        | `npm test` (vitest 4.1.9)  | ✅\*         |
| `tsc --noEmit`                          | **0 errors**                                                        | `npm run typecheck:app`    | ✅           |

\* Верифицировано локально в этой сессии. Для чистого прогона потребовалось восстановить peer `@testing-library/dom` (его роняет `--legacy-peer-deps`); без него 58 RTL-файлов не собираются. CI ставит его через lockfile. → занесено в Sprint 066.

**Топ-10 edge-функций >800 LOC** (реальный бэклог декомпозиции, ни одним спринтом не покрыт):

1. `suno-music-callback/index.ts` — 1330
2. `telegram-bot/handlers/deep-links.ts` — 1327
3. `send-telegram-notification/index.ts` — 1132
4. `klangio-analyze/index.ts` — 1084
5. `ai-lyrics-assistant/prompts.ts` — 1065
6. `generate-track-cover/index.ts` — 1015
7. `telegram-bot/handlers/audio.ts` — 993
8. `telegram-bot/commands/audio-upload.ts` — 948
9. `telegram-bot/commands/analyze.ts` — 841
10. `process-audio-pipeline/index.ts` — 822

---

## 2. Sprint 065 — Generate v2 + Home Redesign + Visual Regression 🔄 В РАБОТЕ

**Что уже в `main`** (последние коммиты: `0db54a6`, `44b8d09`, `34f6c23`, `a32d7b9`, `46bb488`, `f4b6327` и др.):

| #   | Задача                                                     | Файлы                                                                           | Статус |
| --- | ---------------------------------------------------------- | ------------------------------------------------------------------------------- | ------ |
| A1  | Generate v2 — редизайн формы генерации                     | `GenerateFormSimple.tsx`, `CollapsibleFormHeader.tsx`                           | ✅     |
| A2  | Generate v2 — футер и reference-чипы                       | `generate-sheet/GenerateSheetFooter.tsx`, `ReferenceChipsRow.tsx`               | ✅     |
| A3  | Home — мобильный synth-hero                                | `home/HomeMobileSynthHero.tsx` (+190), `pages/Index.tsx`                        | ✅     |
| A4  | Общий brand empty-state                                    | `common/BrandEmptyState.tsx` (+141)                                             | ✅     |
| A5  | Community / Projects таб — выравнивание под редизайн       | `pages/Community.tsx`, `content-hub/tabs/ProjectsTab.tsx`                       | ✅     |
| A6  | Инфраструктура визуальной регрессии                        | `playwright.visual.config.ts`, `tests/visual/home.spec.ts`, `quality-check.yml` | ✅     |
| A7  | Рефактор edge `mcp/index.ts` (256 строк, нетто-сокращение) | `supabase/functions/mcp/index.ts`                                               | ✅     |
| A8  | Миграция БД (`..._65ceef14`)                               | `supabase/migrations/*.sql`                                                     | ✅     |

**Остаток по спринту (closure):**

- [x] **B1** Прогнать `npm run typecheck:app` + `npm test`, зафиксировать реальные цифры — **tsc 0, 1810 passed** (см. §1, §5). Остаётся подтверждение в CI на чистом lockfile.
- [ ] **B2** Записать эталонные снимки визуальной регрессии (`npm run test:visual:home:update`) и проверить их в CI.
- [ ] **B3** Storybook-story для `HomeMobileSynthHero` и `BrandEmptyState` (цель Storybook 50+, сейчас 31).
- [ ] **B4** Обновить `CLAUDE.md`: `Vite 5.0` → фактическая версия из `package.json`, снять устаревшие числа компонентов/хуков.
- [x] **B5** Занести Sprint 065 в `CHANGELOG.md` (`Added`/`Changed`) — ✅ добавлено в `[Unreleased]`.

**Definition of Done:** typecheck 0 · unit-тесты зелёные · визуальные снимки застабилизированы в CI · документация синхронизирована.

---

## 3. Ближайший бэклог — Sprint 066–069

### Sprint 066 — Dependency Health + Build Hygiene 🎯 приоритет №1

Разблокирует локальную установку и снимает риск «сломанной» резолюции.

- [ ] Разрешить конфликт **vite@8 ↔ `@storybook/react-vite@8.6`** (peer требует vite ≤6): либо апгрейд Storybook до Vite-8-совместимого, либо фиксация vite. Цель — чистый `npm ci` без `--legacy-peer-deps`.
- [ ] `npm audit` → закрыть уязвимости (`test:security` = `--audit-level=high` уже в скриптах).
- [ ] Синхронизировать заявленные версии стека в `CLAUDE.md`/`README.md` с `package.json`.
- [ ] Зафиксировать один lockfile (см. давний хвост `bun.lock` vs `package-lock.json`).

### Sprint 067 — Edge Functions Decomposition

Единственная зона с файлами >800 LOC. По одному файлу за раз, под защитой request-validation тестов.

- [ ] `suno-music-callback` (1330) → извлечь обработчики по типу callback.
- [ ] `telegram-bot/handlers/deep-links` (1327) → роутер + per-command модули.
- [ ] `send-telegram-notification` (1132), `klangio-analyze` (1084) — следующая волна.
- [ ] Цель: 0 файлов >800 LOC и в `supabase/functions/`.

### Sprint 068 — Test & Story Coverage

- [ ] Unit: довести до цели после реального замера (файлов тестов уже 184).
- [ ] Обязательные unit-тесты для новых TanStack-Query мутаций (ритуал из retro 052).
- [ ] Storybook: 31 → 50+ stories (добить формы Generate v2 и empty-states).
- [ ] Визуальная регрессия: расширить `tests/visual/` за пределы Home (Library, Studio, Generate).

### Sprint 069 — Bundle + Performance Budgets

- [ ] Total bundle 2.11 MB → ≤1.8 MB (пересмотр `manualChunks`, `npm run size:why`).
- [ ] Lighthouse / Core Web Vitals бюджеты в CI (значится в tech-debt `ROADMAP.md`).
- [ ] Проверка eager JS ≤ 508 KB после редизайна Generate v2/Home.

### Отложенный бэклог (не блокирует)

- WCAG AA пасс по Library + Studio (P2 из аудитов).
- i18n: persist `profiles.locale` (DB-миграция), домены ES/DE.
- Translation pipeline (CI автоперевод), license-compliance workflow.

---

## 4. Таймлайн (июль–август 2026)

```
Неделя 1 (14–20 июля):
  Пн–Вт: Sprint 065 closure (typecheck/тесты/визуальные снимки, docs sync)
  Ср–Пт: Sprint 066 — dependency health (vite↔storybook), npm audit

Неделя 2 (21–27 июля):
  Пн–Ср: Sprint 067 — edge decomposition (suno-music-callback, deep-links)
  Чт–Пт: Sprint 068 — test/story coverage (старт)

Неделя 3 (28 июля – 3 авг):
  Пн–Ср: Sprint 068 — story/visual coverage (добить)
  Чт–Пт: Sprint 069 — bundle reduction + Lighthouse budgets

Неделя 4 (4–8 авг):
  Бэклог: WCAG AA, i18n persist, translation pipeline
```

---

## 5. Заметка о верификации в этой сессии

**Что проверено (в порядке действий):**

1. **Установка зависимостей падает** штатным `npm install` из-за ERESOLVE: корень использует `vite@8.1.4`, а `@storybook/react-vite@8.6.18` требует peer `vite ≤6`. Обход — `npm install --legacy-peer-deps`. → **Sprint 066** (приоритет №1).
2. `--legacy-peer-deps` дополнительно **не ставит peer `@testing-library/dom`** (RTL v16 выносит его в peer): первый прогон дал 58 упавших тест-файлов с `Cannot find module '@testing-library/dom'` — это артефакт установки, **не регрессия кода**.
3. После `npm install --no-save @testing-library/dom` полный прогон зелёный: **`npm test` → 1810 passed** (166 файлов, +2 skip-файла, 4 skip, 31 todo); **`npm run typecheck:app` → 0 errors**.
4. Вывод: код здоров, сломана только резолюция зависимостей в песочнице. CI ставит peer через lockfile (`npm ci`), поэтому у него suite зелёный. Метрики §1 верифицированы напрямую.

> ⚠️ Числа в старых бейджах (1489 / 1497 / 1691 / 1803) — устаревшие снимки. Реальный проверенный показатель на 2026-07-14 — **1810 passed**.

---

## 6. Ритуалы (Definition of Done для спринта)

- **Каждый PR:** typecheck + lint + unit + size-limit.
- **Закрытие спринта:** обновить `PROJECT_STATUS.md`, `SPRINTS/SPRINT-PROGRESS.md`, `ROADMAP.md`, `CHANGELOG.md`, `CLAUDE.md`.
- **Раз в 2 недели:** design review, повторный замер bundle, security audit.
- **Правило анти-дрейфа:** любое число (тесты, спринт, версия) в бейджах документов должно иметь источник — `git`, CI-лог или `find`/`wc`. Расхождение бейджей = баг документации.

---

**Связанные документы:** [PROJECT_STATUS.md](PROJECT_STATUS.md) · [SPRINTS/SPRINT-PROGRESS.md](SPRINTS/SPRINT-PROGRESS.md) · [ROADMAP.md](ROADMAP.md) · [FUTURE_WORK_PLAN.md](FUTURE_WORK_PLAN.md) · [CHANGELOG.md](CHANGELOG.md)
