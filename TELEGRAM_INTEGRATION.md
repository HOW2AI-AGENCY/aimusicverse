# 🤖 Telegram Bot Integration Guide

## Обзор

MusicVerse использует Telegram Mini Apps для предоставления пользователям seamless опыта прямо в Telegram. Аутентификация основана на Telegram Web App initData валидации.

## Архитектура аутентификации

### 1. Frontend (React)

**TelegramContext** (`src/contexts/TelegramContext.tsx`)
- Инициализирует Telegram Web App SDK
- Получает `initData` и информацию о пользователе
- Предоставляет методы для работы с Telegram UI (кнопки, haptic feedback)
- Поддерживает режим разработки для тестирования вне Telegram

**useAuth** (`src/hooks/useAuth.tsx`)
- Управляет процессом аутентификации
- В production использует `telegram-auth` edge function
- В development использует email/password для тестирования
- Проверяет наличие профиля в БД после аутентификации

### 2. Backend (Edge Function)

**telegram-auth** (`supabase/functions/telegram-auth/index.ts`)

Процесс валидации согласно [официальной документации Telegram](https://core.telegram.org/bots/webapps#validating-data-received-via-the-web-app):

```
1. Получить initData от клиента
2. Извлечь hash из initData
3. Создать data_check_string:
   - Удалить hash из параметров
   - Сортировать параметры по ключу
   - URL-декодировать значения
   - Соединить через \n
4. Вычислить secret_key = HMAC_SHA256("WebAppData", bot_token)
5. Вычислить data_hash = HMAC_SHA256(data_check_string, secret_key)
6. Сравнить data_hash с полученным hash
```

## Настройка Telegram Bot

### Шаг 1: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните **Bot Token** (например: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### Шаг 2: Настройка Mini App

1. Отправьте `/newapp` в @BotFather
2. Выберите вашего бота
3. Укажите название и описание
4. Загрузите иконку (можно использовать `public/icon.png`)
5. Укажите URL приложения:
   - Development: `https://lovable.dev/projects/your-project-id`
   - Production: ваш custom domain
6. Настройте Web App URL для кнопки

### Шаг 3: Добавление токена в Lovable Cloud

1. Откройте Lovable Project
2. Перейдите в Settings → Cloud → Secrets
3. Добавьте секрет `TELEGRAM_BOT_TOKEN`
4. Вставьте токен от @BotFather

## Режимы работы

### Development Mode

Автоматически активируется на:
- `*.lovable.dev`
- `*.lovable.app`
- `localhost`
- При добавлении `?dev=1` в URL

**Особенности:**
- Использует email/password аутентификацию
- Создает mock Telegram окружение
- Автоматически создает тестового пользователя
- Все Telegram API вызовы логируются в консоль

### Production Mode

Активируется на кастомных доменах:
- Требует реального Telegram Web App окружения
- Валидирует initData через edge function
- Использует реальный TELEGRAM_BOT_TOKEN

## Безопасность

### Валидация initData

✅ **Правильно:**
```typescript
// 1. Проверяем hash
// 2. Проверяем timestamp (макс. 24 часа)
// 3. Используем HMAC-SHA256 для валидации
// 4. Создаем или обновляем пользователя
```

❌ **Неправильно:**
```typescript
// Доверять данным без проверки hash
// Использовать initData напрямую без валидации
// Игнорировать timestamp
```

### Row Level Security (RLS)

Все таблицы защищены RLS политиками:
```sql
-- Пример политики для профилей
CREATE POLICY "Users can view own profile"
ON profiles FOR SELECT
USING (auth.uid() = user_id);
```

## Типичные проблемы

### 1. "Hash validation failed"

**Причина:** Неправильная валидация или устаревший initData

**Решение:**
- Проверьте, что TELEGRAM_BOT_TOKEN установлен правильно
- Убедитесь, что используется актуальная версия edge function
- Проверьте timestamp - initData не должен быть старше 24 часов

### 2. "No initData available"

**Причина:** Приложение открыто не через Telegram

**Решение:**
- Откройте через Telegram Mini App
- Или используйте development mode для тестирования

### 3. "Invalid Telegram data"

**Причина:** Поврежденные или измененные данные

**Решение:**
- Перезапустите Mini App
- Проверьте, что URL приложения правильно настроен в @BotFather

## Тестирование

### Локально (Development Mode)

```bash
# Запустите проект
npm run dev

# Откройте в браузере
# Development mode активируется автоматически
```

### В Telegram (Production Mode)

1. Опубликуйте приложение через Lovable
2. Настройте Mini App в @BotFather с URL вашего приложения
3. Откройте бота в Telegram
4. Запустите Mini App через меню бота

## Полезные команды

```bash
# Просмотр логов edge function
# В Lovable: Cloud → Functions → telegram-auth → Logs

# Проверка секретов
# В Lovable: Cloud → Settings → Secrets

# Деплой edge function (автоматический при изменениях)
# Изменения в supabase/functions/ автоматически деплоятся
```

## Ресурсы

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
- [Validating initData](https://core.telegram.org/bots/webapps#validating-data-received-via-the-web-app)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)

## Поддержка

При возникновении проблем:
1. Проверьте логи edge function в Lovable Cloud
2. Проверьте browser console для frontend ошибок
3. Убедитесь, что TELEGRAM_BOT_TOKEN установлен корректно
4. Проверьте, что Mini App настроен в @BotFather

---

Последнее обновление: 2025-11-29
