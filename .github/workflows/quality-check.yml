name: Quality & Build

on:
  push:
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Lint / TypeCheck / Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Lint
        run: npm run lint

      - name: Swipe-hint guard
        run: node scripts/check-no-swipe-hints.mjs

      - name: TypeScript check (npm)
        run: npm run typecheck
        env:
          CI: true

      - name: TypeScript check (bun)
        run: bun run typecheck
        env:
          CI: true

      - name: Unit tests
        run: npm test

  build:
    name: Build & Bundle Size
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: quality

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Pre-build type check
        run: npm run typecheck:app
        env:
          CI: true

      - name: Validate CSS @import order
        run: npm run check:css-imports

      - name: Validate Section design tokens
        run: npm run check:section-tokens

      - name: Build
        run: npm run build

      - name: Bundle size check
        run: npm run size

  visual-home:
    name: Visual Regression (Home)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run Home visual regression
        run: npm run test:visual:home
        env:
          CI: true

      - name: Run responsive audit (320-420px + 768px+)
        run: npm run test:responsive
        env:
          CI: true

      - name: Upload visual diff artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: visual-home-diffs
          path: |
            playwright-visual-report
            test-results
          retention-days: 7

  lighthouse:
    name: Lighthouse CI
    runs-on: ubuntu-latest
    timeout-minutes: 10
    needs: build

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build
        run: npm run build

      - name: Run Lighthouse CI
        run: npx lhci autorun
        env:
          LHCI_GITHUB_APP_TOKEN: ${{ secrets.LHCI_GITHUB_APP_TOKEN }}
